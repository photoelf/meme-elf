# meme-elf Roadmap

## Project Shape

`meme-elf` is now a working static browser meme editor with production alpha hosting on `pages.dev`.

The product has three long-term usage modes:
1. `Quick meme`
2. `Image edit++`
3. `Template meme`

The first mode is already live and must remain fast.

## Delivery Principle

Optimize for:
- preserving the fast `Ctrl+V -> text -> Ctrl+C` flow
- adding advanced editing through progressive disclosure
- staying static-host compatible
- keeping operations and deployment simple

## Current Status

### Closed milestones

`Milestone 0: Browser Feasibility`
- closed

`Milestone 1: App Skeleton`
- closed

`Milestone 2: Core Meme Editor`
- closed

`Milestone 3: Desktop Interaction Polish`
- closed for alpha desktop scope

`Milestone 4: Export and Fallback UX`
- closed for current target browsers

`Milestone 5: Alpha Hosting and Operations`
- closed with live `pages.dev` deployment

`Milestone 9: PWA`
- closed for the current alpha target

`Milestone 10: Mobile Optimization`
- closed for the current alpha target

### Working alpha baseline

Current production baseline includes:
- clipboard paste, direct image URL import, and file upload
- multiple editable text layers
- move, resize, rotate, reorder
- image layers with remove, rotate, flip, reorder, and top-of-stack insertion
- pre-insert modal for UI-driven imports and direct image URLs, with crop, rotate, and flip
- scene crop with draggable crop box, resize handles, start-outside-image behavior, and scene image-stack rotate / flip actions
- canvas expansion on any side
- transparent, solid, sampled-edge, and average-border fill for expanded area
- bounds presets for equal margin, caption space, and square canvas
- advanced import from file or clipboard
- inline canvas text editing with multiline support and live re-fit after line removal
- preview zoom with `Fit` as the default load mode, plus `1:1` and middle-mouse pan
- undo / redo on toolbar and physical-key `Ctrl/Cmd+Z` shortcuts
- clipboard copy and PNG download
- production deployment on Cloudflare Pages

## Next Milestones

### Milestone 6: Image Edit++

Goal:
- extend the meme editor with practical image editing without harming the quick meme flow

UX rule:
- advanced image editing lives in the right-side inspector
- default UI remains the fast meme editor

Epics:
1. `Epic 6A: Canvas and Bounds`
2. `Epic 6B: Image Layers and Transforms`
3. `Epic 6C: Effects and Watermarks`

Current status:
- `Epic 6A` is implemented
- `Epic 6B first wave` is implemented
- `Epic 6B post-playtest polish` is closed for desktop alpha scope
- `Epic 6C` is implemented
- `Milestone 6` is complete for the current alpha target
- `Milestone 7` research is complete
- `Epic 10A` is now fixed locally with a single-row phone `preview-toolbar`, the compact header title removed on phone, and touch-safe tooltip focus cleanup after taps
- `Epic 10B` first pass is implemented but reopened for page-scroll-vs-canvas gesture conflicts and mobile focus cleanup
- `Epic 10C` first pass is implemented; `10C-4` is now closed with a shipped long-press export fallback while mobile clipboard capability follow-up continues through research-driven batches
- `10B-2` is now fixed locally with explicit preview touch scroll suppression so active canvas gestures do not fall through to page scroll on phone
- `10B-4` is now fixed locally with status-strip mobile focus cues, outside-tap text-focus dismissal across preview and interface chrome, and canvas-start inline text editing that keeps the phone inspector collapsed until the user explicitly opens `Tools`
- `Epic 10D` is now fixed locally with a dedicated phone crop/expand session tray, compact `Scene transforms` plus `Canvas expand` groupings in the crop inspector, and verified zoom/start-outside-image crop behavior under constrained mobile viewports
- `Epic 10E` is now fixed locally with a dedicated phone retouch session bar for `Draw`, `Erase`, `Pick color`, `Select`, and selection extraction, while experimental retouch now falls back to a desktop-only posture on phone
- `Epic 10F` is now fixed locally with phone-safe layer/effect ordering, raster-stable adjustments and blur, and a compact dedicated watermark preview flow
- `Epic 10G` is now fixed locally with guarded mobile raster preview passes, explicit large-image downscale and recovery messaging, secure-context-aware clipboard fallbacks, and interruption recovery that keeps text editable while flattening non-text content, with a text-only fallback when phone storage is too tight for raster recovery
- `Milestone 10` is complete for the current alpha target
- `Epic 8A` is implemented
- `8B-1` is implemented
- `8B-2` is implemented
- `Epic 8B` is complete for the current milestone path
- next recommended implementation target is `8C-1`

Exit criteria:
- user can perform common meme image surgery without leaving the app

### Milestone 7: Draw and Retouch

Goal:
- let the user paint, erase, and extract parts of an image directly in the editor

Epics:
1. `Epic 7A: Brush and Erase`
2. `Epic 7B: Selection and Extraction`
3. `Epic 7C: Advanced Retouch Research`

Exit criteria:
- user can annotate, obscure, erase, and cut pieces of the meme directly inside the app

Current status:
- `7A-1` is implemented
- `7A-2` is implemented
- `7A-3` is implemented
- `7B-1` is implemented
- `7B-2` is implemented
- `7B-3` is implemented
- `7C-1` is implemented as a shipped local-only narrow clone-stamp tool
- `7C-2` is implemented as a feasibility matrix in the milestone spec
- `7C-3` is implemented as an architecture decision in the milestone spec
- current repo already includes early brush controls and draw UX polish beyond strict `7A-1`
- `Epic 7A` is complete for the current alpha target
- `Epic 7B` is complete for the current alpha target
- `Epic 7C` is complete for the current alpha target with `7C-1` shipped locally as a narrow clone-stamp tool, `7C-2` captured as a feasibility matrix, and `7C-3` closed as `clone stamp custom, healing deferred`
- `Epic 8A` is implemented
- `8B-1` is implemented
- `8B-2` is implemented
- `Epic 8B` is complete for the current milestone path
- next recommended implementation target is `8C-1`

### Milestone 8: Templates and Saves

Goal:
- accelerate recurring meme formats and add reusable local save flows for repeat work

Implementation baseline:
- use the native `.melf` layered package from the start of `8A` as the source of truth for templates, local saves, reopen, and user-shareable work
- keep PSD out of the milestone path entirely; native `.melf` is the only planned save/template format for the current product direction

Epics:
1. `Epic 8A: Template Presets`
2. `Epic 8B: Template Layout Metadata`
3. `Epic 8C: Template UX`
4. `Epic 8D: Local Saves and Reopen Flow`

Current status:
- `8A-1` is implemented
- `8A-2` is implemented
- `Epic 8A` is complete for the current milestone path
- `8B-1` is implemented
- `8B-2` is implemented
- `Epic 8B` is complete for the current milestone path
- `8C-1` is implemented
- `8C-2` is implemented
- `8C-3` is implemented
- `Epic 8C` is complete for the current milestone path
- `8D-0` is implemented
- `8D-1` is implemented
- `8D-2` is implemented
- `8D-3` is implemented
- `Epic 8D` is complete for the current milestone path
- `8E-1` is implemented
- `8E-2` is implemented
- `8E-3` is implemented
- `8E-4` is implemented
- `Epic 8E` is complete for the current milestone path

Exit criteria:
- user can start from a known format, save reusable work locally, and reopen it without rebuilding the layout manually

### Milestone 9: PWA

Goal:
- make `meme-elf` behave like a full installable phone-first web app that launches from the home screen and feels stable in standalone mode

Epics:
1. `Epic 9A: Installability and Web App Manifest`
2. `Epic 9B: Offline Shell and Asset Caching`
3. `Epic 9C: Standalone Mobile Shell and Update UX`
4. `Epic 9D: Real-Device Reliability and Recovery`

Exit criteria:
- user can install the app to the phone home screen, launch it in standalone mode, keep the core editor shell available reliably, and understand when an update is ready without breaking fast meme flow

Current status:
- `Milestone 9` is complete for the current alpha target
- next recommended implementation target is `11A-1`

### Milestone 11: Telegram Mini App

Goal:
- expose `meme-elf` as a Telegram Mini App without forking the editor into a separate product

Route contract:
- `/` remains the normal web and PWA surface
- `/t` is the Telegram-targeted surface
- `/t` can still open outside Telegram without redirecting away, but Telegram-only host actions stay capability-gated

Epics:
1. `Epic 11A: Route Surface and Runtime Contract`
2. `Epic 11B: Telegram SDK Adapter and Lifecycle`
3. `Epic 11C: Telegram Fullscreen Shell and Insets`
4. `Epic 11D: Telegram Export, Share, and Fallback UX`
5. `Epic 11E: Telegram Deployment Surface and Operations`

Exit criteria:
- the app opens correctly inside Telegram, applies Telegram-specific runtime behavior without harming normal web/PWA usage, and has a clear deploy/runtime strategy for the Telegram surface

Current status:
- `11A-1` is implemented
- `11A-2` is implemented
- `11B-1` is implemented
- `11B-2` host normalization is implemented
- `11C-1` route-scoped Telegram shell state is implemented
- `11D-1` export capability policy is implemented
- next recommended implementation target is `11C-2`

## Milestone 6 Breakdown

### Epic 6A: Canvas and Bounds

Deliverables:
- crop
- scene-level rotate `90` and flip for non-text image content
- canvas expansion on any side
- multi-side canvas growth
- transparent, solid, or sampled fill for new canvas area
- smart edge-based fill defaults

Suggested 5-hour batches:
- `6A-1`: crop box model, crop overlay, crop commit and cancel
- `6A-2`: directional canvas expand, transparent and solid fill modes
- `6A-3`: sampled edge color and average border color fill modes
- `6A-4`: multi-side expand presets and layout preservation verification

Implemented so far:
- `6A-1`
- `6A-2`
- `6A-3`
- `6A-4`

### Epic 6B: Image Layers and Transforms

Deliverables:
- add image as layer
- preserve PNG transparency
- place image inside or outside current canvas
- auto-grow canvas on directional outside insertion
- rotate `90`
- flip horizontal or vertical
- move and scale image layers
- pre-insert modal for UI-driven imports
- canvas zoom for advanced editing
- later skew and perspective tools

Suggested 5-hour batches:
- `6B-1`: image layer type model, layer panel support, renderer support
- `6B-2`: import image as new layer with transparency preserved
- `6B-3`: select, move, scale, reorder image layers
- `6B-4`: rotate `90`, flip horizontal, flip vertical
- `6B-5`: insert image outside current canvas with directional canvas growth
- `6B-6A`: pre-insert modal for `Upload Image` and `Advanced import`, with crop and preview transforms
- `6B-6B`: image-layer transform polish, zoom, and preview-stage overlay behavior
- `6B-6`: skew transform UI and renderer plumbing
- `6B-7`: perspective or corner-pin transform prototype

Implemented so far:
- `6B-1` through `6B-5`
- `6B-6A`
- `6B-6B`

### Epic 6C: Effects and Watermarks

Deliverables:
- common image filter stack
- meme degradation effects
- watermark tools
- blend mode foundations where useful

Suggested 5-hour batches:
- `6C-1`: filter state model and non-destructive preview pipeline
- `6C-2`: brightness, contrast, saturation, hue, grayscale, sepia, invert
- `6C-3`: blur, sharpen, threshold, pixelate, noise
- `6C-4`: posterize and JPEG degradation effects
- `6C-5`: watermark generator modes and controls

Implemented so far:
- `6C-1`
- `6C-2`
- `6C-3`
- `6C-4`
- `6C-5`

Known alpha behavior:
- with `Apply to text` disabled, effects apply to base image plus image layers while text stays above the filtered image stack
- with `Apply to text` enabled, text joins the filtered scene pass and can render behind imported image layers; this is accepted for the current alpha because the default remains `off`
- `Adjustments` are fixed-order scene controls, while `Effects` are a reorderable stack whose visible order is also the processing order
- watermark controls live in their own `Watermark` inspector tab, with defaults anchored around an enabled lower-left `создано в программе meme-elf` corner mark at `12px` and optional corner/tile placement controls

Recommended execution order for `6C`:
- `6C-1`
- `6C-2`
- `6C-3`
- `6C-4`
- `6C-5`

## Milestone 7 Breakdown

### Epic 7A: Brush and Erase

Deliverables:
- draw layer
- erase on draw layer
- brush color, size, opacity

Suggested 5-hour batches:
- `7A-1`: draw layer model and brush stroke persistence
- `7A-2`: erase mode and brush controls
- `7A-3`: opacity, soft edge behavior, UX polish

Implemented so far:
- `7A-1`
- `7A-2`
- `7A-3`

Already present in the repo ahead of full milestone closure:
- brush color
- brush size
- brush opacity
- soft-edge toggle
- eyedropper color sampling
- draw-mode UX safeguards that stop accidental layer dragging while painting
- pointer-cancel cleanup for in-flight strokes

### Epic 7B: Selection and Extraction

Deliverables:
- rectangular selection
- copy selection to new layer
- cut selection to new layer

Suggested 5-hour batches:
- `7B-1`: rectangular marquee selection and overlay
- `7B-2`: copy selection to new image layer
- `7B-3`: cut selection to new image layer and hole preservation

Implemented so far:
- `7B-1`
- `7B-2`
- `7B-3`

### Epic 7C: Advanced Retouch Research

Deliverables:
- evaluate clone stamp feasibility
- evaluate healing or patch feasibility
- decide what stays custom and what requires a library

Suggested 5-hour batches:
- `7C-1`: shipped local-only narrow clone stamp on custom stack
- `7C-2`: healing or patch feasibility research with candidate library matrix
- `7C-3`: architecture decision and adoption recommendation

Implemented so far:
- `7C-1`
- `7C-2`
- `7C-3`

Research outcome:
- simple clone stamp is feasible on the current raster stack when sampling stays one-target-at-a-time and reads from a frozen source snapshot during each stroke; the shipped slice currently lives behind the local-only `Experimental` tab
- healing, seamless patch, or content-aware repair are not good custom candidates for the current alpha-plus scope
- no dependency adoption is justified yet; revisit only if a later milestone explicitly needs healing-quality retouch

## Milestone 8 Breakdown

### Epic 8A: Template Presets

Deliverables:
- standard meme starting layouts
- preset text box positions
- native `.melf` template package shape for starter presets

Suggested 5-hour batches:
- `8A-1`: native `.melf` template schema and starter preset set
- `8A-2`: quick-apply flow and preset validation

Implemented so far:
- `8A-1`
- `8A-2`

### Epic 8B: Template Layout Metadata

Deliverables:
- template geometry model
- reusable text and future image slot metadata
- metadata that stays compatible with the native `.melf` save/reopen model

Suggested 5-hour batches:
- `8B-1`: text box geometry serialization
- `8B-2`: image slot and canvas metadata serialization

Implemented so far:
- `8B-1`
- `8B-2`

### Epic 8C: Template UX

Deliverables:
- template picker
- quick apply flow
- fast start into editing

Suggested 5-hour batches:
- `8C-1`: template picker shell and category grouping
- `8C-2`: preview/apply UX and empty-state integration
- `8C-3`: dev-only curator CRUD

Implemented so far:
- `8C-1`
- `8C-2`
- `8C-3`

Notes:
- the shipped local `8C` path now treats templates as built-in meme-template picks with preview tiles in a dedicated inspector `Templates` tab kept outside the preview/canvas main flow during QC
- `8C-3` now exposes localhost-only curator controls inside `Experimental` for importing `.melf` template documents, editing `title` and `tags`, reordering the curated runtime set, and deleting templates without promoting that maintenance UX into the normal editor flow

### Epic 8D: Local Saves and Reopen Flow

Deliverables:
- native `.melf` scene save format
- reopen/edit saved scene flow
- lightweight saved-work management for recurring edits

Suggested 5-hour batches:
- `8D-0`: native-format decision and `.melf` recommendation closeout
- `8D-1`: native `.melf` scene serialization model for local saves
- `8D-2`: native `.melf` save, reopen, and overwrite flow
- `8D-3`: saved-work picker and recovery UX

Implemented so far:
- `8D-0`
- `8D-1`
- `8D-2`
- `8D-3`

Notes:
- `8D-1` now defines a native `.melf` scene document shape separate from template documents, covering canvas size, active layer, embedded base-image payload, editable text/image/draw layers, and scene-wide adjustments/effects/watermark state
- `8D-1` intentionally excludes transient runtime-only editor state such as preview zoom, modal drafts, mobile gesture ownership, retouch drafts, and scene-bounds draft UI state
- `8D-2` now exposes top-bar `Open .melf` and `Save .melf` actions with File System Access API as the preferred browser path, plus hidden file-input and download fallback paths when those APIs are unavailable
- `8D-3` now keeps saved-work UX inside a dedicated inspector `Saves` tab, surfaces recent local `.melf` scenes from browser storage for one-tap reopen, allows stale-entry removal, and exposes interrupted mobile recovery drafts without reintroducing save/template UI into the main preview workspace

### Epic 8E: Shipped Template Catalog and Publish Flow

Deliverables:
- repo-backed shipped template catalog under `public/templates/`
- localhost-only curator promote flow that writes shipped artifacts into the repo
- runtime template picker loading from shipped catalog assets instead of hardcoded starter presets
- documented curator-to-commit publish path

Suggested 5-hour batches:
- `8E-1`: repo catalog contract
- `8E-2`: dev-only promote endpoint
- `8E-3`: runtime shipped catalog loader and picker cutover
- `8E-4`: publish closeout and docs

Implemented so far:
- `8E-1`
- `8E-2`
- `8E-3`
- `8E-4`

Notes:
- the shipped template picker now reads repo-backed assets from `public/templates/` and currently ships the curated `Two Buttons` template instead of the earlier starter preset set
- localhost `Experimental` keeps draft-only curator controls for import, title/tag edits, reordering, delete, and `Promote shipped catalog`, while the normal `Templates` tab stays bound to the shipped catalog
- `Promote shipped catalog` is the explicit seam between browser-local draft state and production-visible built-in templates

## Milestone 9 Breakdown

### Epic 9A: Installability and Web App Manifest

Deliverables:
- stable web app manifest
- installable icons and metadata
- explicit standalone launch behavior
- install prompt posture that does not pollute the quick flow

Suggested 5-hour batches:
- `9A-1`: manifest audit and icon asset matrix
- `9A-2`: standalone launch metadata and installability validation
- `9A-3`: install entrypoint UX and fallback copy

### Epic 9B: Offline Shell and Asset Caching

Deliverables:
- service worker baseline
- cached app shell for repeat launches
- explicit policy for editor assets versus user-generated content
- safe cache invalidation rules

Suggested 5-hour batches:
- `9B-1`: service worker/runtime selection and scope contract
- `9B-2`: static shell precache and asset versioning
- `9B-3`: offline-state messaging and network-boundary rules

### Epic 9C: Standalone Mobile Shell and Update UX

Deliverables:
- standalone-safe top bar and viewport behavior
- launch-from-icon session posture
- update-available and refresh flow
- no confusing browser-chrome assumptions in standalone mode

Suggested 5-hour batches:
- `9C-1`: standalone shell audit and CSS/runtime tweaks
- `9C-2`: update detection and refresh affordance
- `9C-3`: standalone-specific regression sweep across import/export flows

### Epic 9D: Real-Device Reliability and Recovery

Deliverables:
- phone install validation on real devices
- interruption/resume expectations for installed mode
- documented browser-specific known limits
- app-icon path proven on the user’s target devices

Suggested 5-hour batches:
- `9D-1`: Android install and launch validation
- `9D-2`: iPhone add-to-home-screen validation
- `9D-3`: recovery, storage, and re-entry behavior closeout

## Milestone 11 Breakdown

### Epic 11A: Route Surface and Runtime Contract

Deliverables:
- `/t` Telegram route on the same SPA
- Telegram-aware runtime detection
- clear boundary between `/` and `/t`
- no accidental Telegram-only assumptions in the default app path

Suggested 5-hour batches:
- `11A-1`: route contract and app-mode model
- `11A-2`: shared bootstrap with Telegram route seam
- `11A-3`: `/t` non-Telegram fallback posture and smoke validation

### Epic 11B: Telegram SDK Adapter and Lifecycle

Deliverables:
- Telegram SDK injection path
- theme, viewport, and lifecycle bridge
- explicit isolation between Telegram host APIs and the core editor

Suggested 5-hour batches:
- `11B-1`: SDK loader and readiness state
- `11B-2`: host adapter for lifecycle, theme, and navigation hooks
- `11B-3`: app integration through a shared host interface

### Epic 11C: Telegram Fullscreen Shell and Insets

Deliverables:
- fullscreen-safe layout
- Telegram viewport and inset handling
- CSS tweaks for Telegram chrome and safe areas
- stable editor behavior under Telegram-specific resize events

Suggested 5-hour batches:
- `11C-1`: fullscreen shell audit for current mobile layout
- `11C-2`: Telegram viewport and inset CSS bridge
- `11C-3`: focused regression pass for keyboard, panels, and export controls

### Epic 11D: Telegram Export, Share, and Fallback UX

Deliverables:
- Telegram-aware export/share options
- fallback posture when clipboard/download behavior differs inside Telegram
- explicit user-facing outcome after export

Suggested 5-hour batches:
- `11D-1`: export capability matrix and route-specific action policy
- `11D-2`: Telegram-oriented share and fallback UX
- `11D-3`: in-host validation and known-limits closeout

### Epic 11E: Telegram Deployment Surface and Operations

Deliverables:
- concrete deploy strategy for Telegram surface
- clear hosting/routing rule for Telegram requests
- docs for how Telegram mode is enabled and validated

Suggested 5-hour batches:
- `11E-1`: route and hosting contract
- `11E-2`: Telegram registration and config guide
- `11E-3`: release checklist and known limits

## Technology Direction

Default approach:
- keep using React + TypeScript + browser canvas
- evaluate new libraries only when they unlock a clear feature cluster

Strong candidates for custom implementation:
- crop
- canvas expansion
- image transforms
- image layering
- draw and erase
- rectangular selection
- common filters
- watermarking

Likely evaluation areas before adopting dependencies:
- advanced perspective tooling
- advanced retouch and inpaint-like workflows
- more complex filter pipelines

## Recommended Near-Term Sequence

1. `M6 / Epic 6A`
- crop
- expand canvas
- smart fill defaults

2. `M6 / Epic 6C`
- basic effects
- watermarks
- degradation tools
  status: complete for current alpha target

3. `M7 / Epic 7A`
- brush and erase

4. `M7 / Epic 7B`
- selection and extraction

5. `M7 / Epic 7C`
- retouch research

6. `M10`
- mobile optimization before new workflow complexity

7. `M8`
- templates
- local saves and reopen flows

8. `M9`
- PWA installability
- standalone mobile app-shell hardening

9. `M11`
- Telegram Mini App surface
- Telegram-specific runtime integration

## What Not To Pull Forward Yet

Do not automatically fold these into the next phase:
- auth
- cloud save
- public gallery
- AI generation
- animation or video

## Source Documents

Current planning should stay aligned with:
- [design.md](/D:/PETS/meme-elf/design.md)
- [docs/superpowers/specs/2026-06-04-meme-generator-alpha-design.md](/D:/PETS/meme-elf/docs/superpowers/specs/2026-06-04-meme-generator-alpha-design.md)
- [docs/superpowers/specs/2026-06-05-post-alpha-editor-evolution-design.md](/D:/PETS/meme-elf/docs/superpowers/specs/2026-06-05-post-alpha-editor-evolution-design.md)
- [docs/superpowers/specs/2026-06-05-m6-post-playtest-polish-design.md](/D:/PETS/meme-elf/docs/superpowers/specs/2026-06-05-m6-post-playtest-polish-design.md)
